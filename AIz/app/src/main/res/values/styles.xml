<!-- res/values/styles.xml -->
<resources>

    <!-- Make sure your AppTheme inherits from a Material Components theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- primary blue, on-primary white -->
        <item name="colorPrimary">@color/blue_600</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/blue_700</item>
    </style>

    <!-- Our custom button style -->
    <style name="MyButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:insetLeft">16dp</item>
        <item name="android:insetRight">16dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>

        <!-- Make sure text is white -->
        <item name="android:textColor">@color/white</item>
        <!-- Tint the button background blue -->
        <item name="backgroundTint">@color/blue_600</item>
    </style>

</resources>
